import os # Import the OS module to interact with the Operating System

# The listdir() function returns a list of all files and folders in the current directory
# We print this list to the console
print(os.listdir())